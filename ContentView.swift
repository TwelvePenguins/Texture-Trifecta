import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .font(.body)
                .foregroundColor(.accentColor)
            Text("Hello, world!")
        }
    }
}
